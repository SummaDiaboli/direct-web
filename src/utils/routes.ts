enum Routes {
    HOME = "/",
    WHY = "/why-direct",
    HOW = "/how-it-works",
    DOWNLOAD = "/download",
    SIGNUP = "/sign-up",
    LOGIN = "/login",
    MAGICPAGE = "/sign-up/:id",
    QRPAGE = "/login/:id"
}

export default Routes;
